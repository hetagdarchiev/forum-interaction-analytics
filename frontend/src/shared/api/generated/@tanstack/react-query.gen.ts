// This file is auto-generated by @hey-api/openapi-ts

import {
  type DefaultError,
  type InfiniteData,
  infiniteQueryOptions,
  queryOptions,
  type UseMutationOptions,
} from '@tanstack/react-query';

import { client } from '../client.gen';
import {
  analyticsMetricsGet,
  analyticsVisitBatchSubmit,
  authLogin,
  authLogout,
  mediaGet,
  mediaUpload,
  type Options,
  threadAddPost,
  threadCreate,
  threadGet,
  threadsList,
  userCreate,
  userDelete,
  userGet,
  userMe,
  userUpdate,
} from '../sdk.gen';
import type {
  AnalyticsMetricsGetData,
  AnalyticsMetricsGetError,
  AnalyticsMetricsGetResponse,
  AnalyticsVisitBatchSubmitData,
  AnalyticsVisitBatchSubmitError,
  AnalyticsVisitBatchSubmitResponse,
  AuthLoginData,
  AuthLoginError,
  AuthLoginResponse,
  AuthLogoutData,
  AuthLogoutResponse,
  MediaGetData,
  MediaGetError,
  MediaGetResponse,
  MediaUploadData,
  MediaUploadError,
  MediaUploadResponse2,
  ThreadAddPostData,
  ThreadAddPostError,
  ThreadAddPostResponse,
  ThreadCreateData,
  ThreadCreateError,
  ThreadCreateResponse,
  ThreadGetData,
  ThreadGetError,
  ThreadGetResponse,
  ThreadsListData,
  ThreadsListError,
  ThreadsListResponse,
  UserCreateData,
  UserCreateError,
  UserCreateResponse2,
  UserDeleteData,
  UserDeleteResponse,
  UserGetData,
  UserGetError,
  UserGetResponse2,
  UserMeData,
  UserMeError,
  UserMeResponse,
  UserUpdateData,
  UserUpdateResponse,
} from '../types.gen';

/**
 * Create a new user
 */
export const userCreateMutation = (
  options?: Partial<Options<UserCreateData>>,
): UseMutationOptions<
  UserCreateResponse2,
  UserCreateError,
  Options<UserCreateData>
> => {
  const mutationOptions: UseMutationOptions<
    UserCreateResponse2,
    UserCreateError,
    Options<UserCreateData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await userCreate({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export type QueryKey<TOptions extends Options> = [
  Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
    _id: string;
    _infinite?: boolean;
    tags?: ReadonlyArray<string>;
  },
];

const createQueryKey = <TOptions extends Options>(
  id: string,
  options?: TOptions,
  infinite?: boolean,
  tags?: ReadonlyArray<string>,
): [QueryKey<TOptions>[0]] => {
  const params: QueryKey<TOptions>[0] = {
    _id: id,
    baseUrl:
      options?.baseUrl || (options?.client ?? client).getConfig().baseUrl,
  } as QueryKey<TOptions>[0];
  if (infinite) {
    params._infinite = infinite;
  }
  if (tags) {
    params.tags = tags;
  }
  if (options?.body) {
    params.body = options.body;
  }
  if (options?.headers) {
    params.headers = options.headers;
  }
  if (options?.path) {
    params.path = options.path;
  }
  if (options?.query) {
    params.query = options.query;
  }
  return [params];
};

export const userMeQueryKey = (options?: Options<UserMeData>) =>
  createQueryKey('userMe', options);

/**
 * Get current user information
 */
export const userMeOptions = (options?: Options<UserMeData>) =>
  queryOptions<
    UserMeResponse,
    UserMeError,
    UserMeResponse,
    ReturnType<typeof userMeQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await userMe({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: userMeQueryKey(options),
  });

/**
 * Delete a user
 */
export const userDeleteMutation = (
  options?: Partial<Options<UserDeleteData>>,
): UseMutationOptions<
  UserDeleteResponse,
  DefaultError,
  Options<UserDeleteData>
> => {
  const mutationOptions: UseMutationOptions<
    UserDeleteResponse,
    DefaultError,
    Options<UserDeleteData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await userDelete({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const userGetQueryKey = (options: Options<UserGetData>) =>
  createQueryKey('userGet', options);

/**
 * Get user information
 */
export const userGetOptions = (options: Options<UserGetData>) =>
  queryOptions<
    UserGetResponse2,
    UserGetError,
    UserGetResponse2,
    ReturnType<typeof userGetQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await userGet({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: userGetQueryKey(options),
  });

/**
 * Update user information
 */
export const userUpdateMutation = (
  options?: Partial<Options<UserUpdateData>>,
): UseMutationOptions<
  UserUpdateResponse,
  DefaultError,
  Options<UserUpdateData>
> => {
  const mutationOptions: UseMutationOptions<
    UserUpdateResponse,
    DefaultError,
    Options<UserUpdateData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await userUpdate({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * User login
 *
 * Create access and refresh JWT tokens, send to user. The refresh token also stored in a cookie.
 *
 */
export const authLoginMutation = (
  options?: Partial<Options<AuthLoginData>>,
): UseMutationOptions<
  AuthLoginResponse,
  AuthLoginError,
  Options<AuthLoginData>
> => {
  const mutationOptions: UseMutationOptions<
    AuthLoginResponse,
    AuthLoginError,
    Options<AuthLoginData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await authLogin({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * User logout
 */
export const authLogoutMutation = (
  options?: Partial<Options<AuthLogoutData>>,
): UseMutationOptions<
  AuthLogoutResponse,
  DefaultError,
  Options<AuthLogoutData>
> => {
  const mutationOptions: UseMutationOptions<
    AuthLogoutResponse,
    DefaultError,
    Options<AuthLogoutData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await authLogout({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const threadsListQueryKey = (options?: Options<ThreadsListData>) =>
  createQueryKey('threadsList', options);

/**
 * Get list of threads with pagination
 *
 * Получить список веток с пагинацией. Можно использовать либо постраничную пагинацию (page + limit),
 * либо курсорную пагинацию (after или before). Нужно использовать только один параметр.
 * after, before или page с номером страницы. Если ни один не указан - выводятся самые свежие сообщения.
 * limit - количество сообщений на страницу, по умолчанию 20.
 * С разделением на страницы есть неприятная особенность. При удалении или добавлении новых сообщений,
 * страницы могут "прыгать". Т.е. у нас есть список (сообщений) и в него могут добавляться и удаляться элементы
 * в любом месте списка. Если мы находится на странице 3 и хотим 7-ю, то в ней могут быть совсем другие элементы,
 * чем на момент запроса страницы 3. Поэтому для более стабильной пагинации можно использовать курсорную пагинацию.
 *
 * Навигация по номеру страницы выберает все сообщения на момент запроса и отдает нужную страницу. Добавление
 * или удаление сообщений сбивает это разделение.
 *
 * Курсорная пагинация позволяет двигаться вперед и назад по списку, учитывая изменеия в нем.
 * Но для нее нужно указывать минимальный или максимальный id сообщения на странице, чтобы понять откуда двигаться
 * дальше. И она не позволяет прыгать на конкретную страницу, а только двигаться вперед и назад.
 * При этом before и after не включаются в результат, т.е. если указать before=10, то в результат не попадет
 * сообщение с id 10, а только с id меньше 10. И аналогично для after. В них указываются id сообщения, но
 * before - для получения более старых сообщений, а after - для получения более новых сообщений по времени.
 * Более старым сообщениям (before) соответствует меньший id (более старые сообщения),
 * а более новым (after) - больший id. И при этом не важно, удалены эти сообщения или нет.
 *
 */
export const threadsListOptions = (options?: Options<ThreadsListData>) =>
  queryOptions<
    ThreadsListResponse,
    ThreadsListError,
    ThreadsListResponse,
    ReturnType<typeof threadsListQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await threadsList({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: threadsListQueryKey(options),
  });

const createInfiniteParams = <
  K extends Pick<QueryKey<Options>[0], 'body' | 'headers' | 'path' | 'query'>,
>(
  queryKey: QueryKey<Options>,
  page: K,
) => {
  const params = { ...queryKey[0] };
  if (page.body) {
    params.body = {
      ...(queryKey[0].body as any),
      ...(page.body as any),
    };
  }
  if (page.headers) {
    params.headers = {
      ...queryKey[0].headers,
      ...page.headers,
    };
  }
  if (page.path) {
    params.path = {
      ...(queryKey[0].path as any),
      ...(page.path as any),
    };
  }
  if (page.query) {
    params.query = {
      ...(queryKey[0].query as any),
      ...(page.query as any),
    };
  }
  return params as unknown as typeof page;
};

export const threadsListInfiniteQueryKey = (
  options?: Options<ThreadsListData>,
): QueryKey<Options<ThreadsListData>> =>
  createQueryKey('threadsList', options, true);

/**
 * Get list of threads with pagination
 *
 * Получить список веток с пагинацией. Можно использовать либо постраничную пагинацию (page + limit),
 * либо курсорную пагинацию (after или before). Нужно использовать только один параметр.
 * after, before или page с номером страницы. Если ни один не указан - выводятся самые свежие сообщения.
 * limit - количество сообщений на страницу, по умолчанию 20.
 * С разделением на страницы есть неприятная особенность. При удалении или добавлении новых сообщений,
 * страницы могут "прыгать". Т.е. у нас есть список (сообщений) и в него могут добавляться и удаляться элементы
 * в любом месте списка. Если мы находится на странице 3 и хотим 7-ю, то в ней могут быть совсем другие элементы,
 * чем на момент запроса страницы 3. Поэтому для более стабильной пагинации можно использовать курсорную пагинацию.
 *
 * Навигация по номеру страницы выберает все сообщения на момент запроса и отдает нужную страницу. Добавление
 * или удаление сообщений сбивает это разделение.
 *
 * Курсорная пагинация позволяет двигаться вперед и назад по списку, учитывая изменеия в нем.
 * Но для нее нужно указывать минимальный или максимальный id сообщения на странице, чтобы понять откуда двигаться
 * дальше. И она не позволяет прыгать на конкретную страницу, а только двигаться вперед и назад.
 * При этом before и after не включаются в результат, т.е. если указать before=10, то в результат не попадет
 * сообщение с id 10, а только с id меньше 10. И аналогично для after. В них указываются id сообщения, но
 * before - для получения более старых сообщений, а after - для получения более новых сообщений по времени.
 * Более старым сообщениям (before) соответствует меньший id (более старые сообщения),
 * а более новым (after) - больший id. И при этом не важно, удалены эти сообщения или нет.
 *
 */
export const threadsListInfiniteOptions = (
  options?: Options<ThreadsListData>,
) =>
  infiniteQueryOptions<
    ThreadsListResponse,
    ThreadsListError,
    InfiniteData<ThreadsListResponse>,
    QueryKey<Options<ThreadsListData>>,
    | number
    | Pick<
        QueryKey<Options<ThreadsListData>>[0],
        'body' | 'headers' | 'path' | 'query'
      >
  >(
    // @ts-ignore
    {
      queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<
          QueryKey<Options<ThreadsListData>>[0],
          'body' | 'headers' | 'path' | 'query'
        > =
          typeof pageParam === 'object'
            ? pageParam
            : {
                query: {
                  page: pageParam,
                },
              };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await threadsList({
          ...options,
          ...params,
          signal,
          throwOnError: true,
        });
        return data;
      },
      queryKey: threadsListInfiniteQueryKey(options),
    },
  );

/**
 * Create a new thread
 */
export const threadCreateMutation = (
  options?: Partial<Options<ThreadCreateData>>,
): UseMutationOptions<
  ThreadCreateResponse,
  ThreadCreateError,
  Options<ThreadCreateData>
> => {
  const mutationOptions: UseMutationOptions<
    ThreadCreateResponse,
    ThreadCreateError,
    Options<ThreadCreateData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await threadCreate({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const threadGetQueryKey = (options: Options<ThreadGetData>) =>
  createQueryKey('threadGet', options);

/**
 * Get single thread with all posts by thread id
 */
export const threadGetOptions = (options: Options<ThreadGetData>) =>
  queryOptions<
    ThreadGetResponse,
    ThreadGetError,
    ThreadGetResponse,
    ReturnType<typeof threadGetQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await threadGet({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: threadGetQueryKey(options),
  });

/**
 * Add a new post to thread
 */
export const threadAddPostMutation = (
  options?: Partial<Options<ThreadAddPostData>>,
): UseMutationOptions<
  ThreadAddPostResponse,
  ThreadAddPostError,
  Options<ThreadAddPostData>
> => {
  const mutationOptions: UseMutationOptions<
    ThreadAddPostResponse,
    ThreadAddPostError,
    Options<ThreadAddPostData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await threadAddPost({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Submit a client-side analytics batch (durations, device, activity hints)
 *
 * Public endpoint; when the `sid` cookie is present, the batch is linked to the user.
 * Idempotent per `clientBatchId`.
 *
 */
export const analyticsVisitBatchSubmitMutation = (
  options?: Partial<Options<AnalyticsVisitBatchSubmitData>>,
): UseMutationOptions<
  AnalyticsVisitBatchSubmitResponse,
  AnalyticsVisitBatchSubmitError,
  Options<AnalyticsVisitBatchSubmitData>
> => {
  const mutationOptions: UseMutationOptions<
    AnalyticsVisitBatchSubmitResponse,
    AnalyticsVisitBatchSubmitError,
    Options<AnalyticsVisitBatchSubmitData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await analyticsVisitBatchSubmit({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const analyticsMetricsGetQueryKey = (
  options?: Options<AnalyticsMetricsGetData>,
) => createQueryKey('analyticsMetricsGet', options);

/**
 * Aggregated analytics (requires login)
 */
export const analyticsMetricsGetOptions = (
  options?: Options<AnalyticsMetricsGetData>,
) =>
  queryOptions<
    AnalyticsMetricsGetResponse,
    AnalyticsMetricsGetError,
    AnalyticsMetricsGetResponse,
    ReturnType<typeof analyticsMetricsGetQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await analyticsMetricsGet({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: analyticsMetricsGetQueryKey(options),
  });

/**
 * Upload media file
 */
export const mediaUploadMutation = (
  options?: Partial<Options<MediaUploadData>>,
): UseMutationOptions<
  MediaUploadResponse2,
  MediaUploadError,
  Options<MediaUploadData>
> => {
  const mutationOptions: UseMutationOptions<
    MediaUploadResponse2,
    MediaUploadError,
    Options<MediaUploadData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await mediaUpload({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const mediaGetQueryKey = (options: Options<MediaGetData>) =>
  createQueryKey('mediaGet', options);

/**
 * Get media file by name
 */
export const mediaGetOptions = (options: Options<MediaGetData>) =>
  queryOptions<
    MediaGetResponse,
    MediaGetError,
    MediaGetResponse,
    ReturnType<typeof mediaGetQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await mediaGet({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: mediaGetQueryKey(options),
  });
