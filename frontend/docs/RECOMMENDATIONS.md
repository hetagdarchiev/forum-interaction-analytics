## 🧩 Правила импорта

Для поддержания чистоты путей и легкого перемещения файлов используйте алиасы:

* **Используйте `@/`** для всех импортов из других модулей или папок.
    * *Правильно:* `import { Button } from "@/shared/ui/button"`
* **Относительные пути `./`** разрешены **только** для файлов, находящихся в той же самой папке.
    * *Правильно:* `import { styles } from "./styles.module.css"`

---

## 💎 Чистота кода и именование

### 1. Никаких сокращений

Всегда пишите полные имена для переменных, функций, файлов и папок. Избегайте аббревиатур, смысл которых может быть
неясен.

| Плохо (❌)                            | Хорошо (✅)                                   |
|:-------------------------------------|:---------------------------------------------|
| `acc-list.tsx`                       | `accordion-list.tsx`                         |
| `const res = fetch(...)`             | `const response = fetch(...)`                |
| `const handleOpen = (e) => e.target` | `const handleOpen = (event) => event.target` |
| `const [val, setVal] = ...`          | `const [value, setValue] = ...`              |

### 2. Избегайте абстрактных имен

Глобальные переменные и пропсы должны четко описывать, что внутри. Не используйте «пустые» слова вроде `data`, `list`
или `item` в одиночку.

* **❌ Плохо:** `const list = []`, `data.map(item => ...)`
* **✅ Хорошо:** `const commentsList = []`, `forumTopics.map(topic => ...)`

### 3. Обработка событий

Для функций-обработчиков используйте префикс `handle` или четкое действие.

* *Пример:* `const handlePreventDefault = (event) => { event.preventDefault(); ... }`